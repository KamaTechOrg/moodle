<?php
defined('MOODLE_INTERNAL') || die();

$plugin->component = 'local_mdl_capsule';  // Full name of the plugin (used for diagnostics).
$plugin->version   = 2025051400;       // Plugin version (Date: YYYYMMDDXX).
$plugin->requires  = 2020110900;       // Moodle version required (this example requires Moodle 3.10).
$plugin->maturity  = MATURITY_STABLE;  // Plugin maturity level (STABLE, ALPHA, BETA, etc.).
$plugin->release   = '1.0';            // Release version (can be incremented).
